node_modules/
dist/
build/
.env
.env.local
*.log
# Redirected stderr from a locally started service. Two empty ones reached
# main in #47 via `git add -A`; *.log was ignored and *.err was not.
*.err
.DS_Store
.pnpm-store/
coverage/
# Trained models are big and get regenerated - keep them out.
ml/models/*.onnx
!ml/models/.gitkeep
# ...except a small committed baseline, so both runtimes, the parity test and
# the field view work end to end from a clean clone with no training step.
!ml/models/baseline-*.onnx
data/firms-cache/
.vite/

# Presentation artefacts are built locally and sent directly, not versioned.
Charkha-HashHawks-*.pptx
Charkha-HashHawks-*.pdf
